
lec38
1. Create a dir called "tf_mod_pro"
2. create a dir in it called "modules"
3. create a dir in the modules dir "s3"
4. in the s3 dir create a main.tf file 
5. create s3 with prefix "task1526"
6. create a folders in the s3 with names "videos","images"
7. use the s3 module in your root module

lec39
1. create a variable to the use to select if he need SSE-S3 or SSE-KMS https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/s3_bucket_server_side_encryption_configuration

tf workspace --help
Usage: terraform [global options] workspace

  new, list, show, select and delete Terraform workspaces.

Subcommands:
    delete    Delete a workspace
    list      List Workspaces
    new       Create a new workspace
    select    Select a workspace
    show      Show the name of the current workspace
https://kodekloud.com/studio/labs/terraform/terraform-workspaces