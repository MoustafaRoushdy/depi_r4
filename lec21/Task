1. Install prometheus as a docker container
   a. mount the config file from host to container ( see docs )
2. start two servers, install prometheus node exporter on them 
3. edit the config file to allow prometheu to monitor these two servers
