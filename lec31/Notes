git remote add <alias> repourl 
git push --set-upstream <alias> <branch>  => for only the first time and then you can only use "git push"


git clone <remote url> => to clone a remote repo to your localmachine
git remote -v => to see remote urls
